<% if @calculation.errors.any? %>
  $("#flash").html('<%= j render partial: "layouts/messages" %>');
<% else %>
  $('textarea#calculation_result').html("<%= escape_javascript render(partial: 'result', locals: { calculation: @calculation, prev_calc: @prev_calc }) %>");
<% end %>